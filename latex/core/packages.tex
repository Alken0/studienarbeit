% !TEX root = ../main.tex
% !TeX spellcheck = de_DE

% character encoding
\usepackage[utf8]{inputenc}

% page definition
\usepackage[
	top=25mm,
	bottom=25mm,
	left=25mm,
	right=25mm,
	includehead,
	includefoot,
	headsep=1cm,
]{geometry}

% Header/Footer
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\sffamily \theAuthor}
\fancyhead[R]{\sffamily \nouppercase{\leftmark}}
\fancyfoot[C]{\sffamily \thepage}

% stuff
\usepackage[T1]{fontenc}
\usepackage{lmodern}

% Linespace
\usepackage[onehalfspacing]{setspace}

% Math
\usepackage{neuralnetwork} 

\usepackage{amssymb}

% Language = German
\usepackage[ngerman]{babel}
\usepackage[
	babel, 
	german=quotes,
]{csquotes}

% Graphics
\usepackage{graphicx}
\usepackage{float}
\usepackage{subcaption} % multiple pictures in one row

% Urls
\usepackage{url}

% package for todos
\usepackage{todonotes}

% Lists
\usepackage{enumitem}
%\usepackage{listings}

% comment out multiple lines using \begin{comment}...
\usepackage{comment}

% must have: http://tug.ctan.org/tex-archive/macros/latex/contrib/microtype/
\usepackage{microtype}

% citation, bibliography
\usepackage[
	backend=biber,
	sorting=none,
	citestyle = numeric-comp
]{biblatex}
%moved to main because TexStudio doesn't show it in sidebar otherwise:
  %\addbibresource{references.bib}

%%%% have to be last and in this order %%%%
\usepackage{hyperref}

\usepackage[
	toc, 
	acronym,
]{glossaries}
%moved to main because TexStudio doesn't show it in sidebar otherwise:
  %\input{glossaries}
  %\makeglossaries

\usepackage[german]{cleveref}
